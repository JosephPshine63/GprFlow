# Copy to .env and fill in real values. Never commit the real .env file.
# For local `mvn spring-boot:run`, load this with: set -a; source .env; set +a

# Gemini AI
GEMINI_API_KEY=your-gemini-api-key
