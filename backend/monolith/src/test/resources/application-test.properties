# Fresh Testcontainers DB has no pre-existing schema, so Hibernate must create it
# (base application.properties uses ddl-auto=validate, which assumes Liquibase-managed schema).
spring.jpa.hibernate.ddl-auto=update
