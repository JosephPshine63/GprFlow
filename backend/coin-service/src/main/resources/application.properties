spring.application.name=coin-service

server.port=${SERVER_PORT:5455}

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/gprflow?currentSchema=coin}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:postgres}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=validate

# JWT secret — must match the monolith's JWT_SECRET so tokens issued there validate here
jwt.secret=${JWT_SECRET:wpembytrwcvnryxksdbqwjebruyGHyudqgwveytrtrCSnwifoesarjbwe}
jwt.expiration.ms=${JWT_EXPIRATION_MS:86400000}

# coingecko api key
coingecko.api.key=${COINGECKO_API_KEY:your coin gecko api key}
